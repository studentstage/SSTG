# AI Assistant Boundary

This directory is intentionally an extension point only. No provider, client,
API key, fake response, or undocumented endpoint belongs here until a backend
AI contract is approved. Future secret-bearing integrations should run through
a backend gateway rather than browser-exposed environment variables.
